import React, { Component } from 'react';
import Cells from '../theme/css';

class Cell extends Component {
    render() {
        return(
        <Cells/>
        )
    }
}

export default Cell;