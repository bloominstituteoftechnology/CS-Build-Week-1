import React from 'react';

const Buttons = () => {
  return (
    <div className="buttons">
      <button>Start</button>
      <button>Stop</button>
      <button>Clear</button>
    </div>
  );
};

export default Buttons;