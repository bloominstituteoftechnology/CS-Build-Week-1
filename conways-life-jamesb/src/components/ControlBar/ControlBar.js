import React from 'react';

export default function Grid(props) {
    return (
        <>
            <p>CONTROL BAR</p>
            {/* <button onClick={props.createGrid()} /> */}
        </>
    )
}